# HTTP
PORT=3000

# SSE
HEARTBEAT_MS=20000
SSE_RETRY_MS=2000

# Buffer (in-memory ring buffer)
RING_BUFFER_SIZE=10000

# Provider consumer reconnect (max backoff in ms)
PROVIDER_RECONNECT_MAX_MS=30000
